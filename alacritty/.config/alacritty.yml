# Live config reload (changes require restart)
live_config_reload: true


# Startup directory
# Directory the shell is started in. If this is unset, or `None`, the working
# directory of the parent process will be used.
working_directory: None


# Offer IPC using `alacritty msg` (unix only)
ipc_socket: true


# Import additional configuration files
#
# Imports are loaded in order, skipping all missing files, with the importing
# file being loaded last. If a field is already present in a previous import, it
# will be replaced.
#
# All imports must either be absolute paths starting with `/`, or paths relative
# to the user's home directory starting with `~/`.
import:
  - ~/.config/alacritty-config/bell.yaml
  - ~/.config/alacritty-config/colors.yaml
  - ~/.config/alacritty-config/cursor.yaml
  - ~/.config/alacritty-config/debug.yaml
  - ~/.config/alacritty-config/env.yaml
  - ~/.config/alacritty-config/font.yaml
  - ~/.config/alacritty-config/hints.yaml
  - ~/.config/alacritty-config/keymaps.yaml
  - ~/.config/alacritty-config/mouse.yaml
  - ~/.config/alacritty-config/scrolling.yaml
  - ~/.config/alacritty-config/selection.yaml
  - ~/.config/alacritty-config/shell.yaml
  - ~/.config/alacritty-config/window.yaml
